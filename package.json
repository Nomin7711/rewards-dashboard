{
  "name": "rewards-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Resident rewards dashboard",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "install:all": "npm install && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
